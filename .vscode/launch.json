{
    "configurations": [
        {
            "name": "http",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "envFile": "${workspaceFolder}/.env",
            "program": "${workspaceFolder}/cmd/http/main.go",
            "cwd": "${workspaceFolder}"
        }
    ]
}