info:
  name: Search Content
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/content/search"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
