info:
  name: Search Content
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/content/search?q=spongebob"
  params:
    - name: q
      value: spongebob
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
